{
  "name": "live-update-provider-mock",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "android",
    "!android/.gradle",
    "!android/.idea",
    "!android/.kotlin",
    "!android/**/build",
    "!android/local.properties",
    "dist",
    "ios/Sources",
    "src",
    "Package.swift",
    "Package.resolved"
  ],
  "capacitor": {
    "ios": {
      "src": "ios/Sources/LiveUpdateProviderMock"
    },
    "android": {
      "src": "android/live-update-provider-mock"
    }
  },
  "scripts": {
    "build": "tsc",
    "build:android": "cd android && ./gradlew build"
  },
  "peerDependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
