{
  "name": "ionic-sunrise-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:portals": "BUILD_PATH=build-portals react-scripts build",
    "build:fedcap": "BUILD_PATH=build-fedcap craco build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "@craco/craco": "^7.1.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
